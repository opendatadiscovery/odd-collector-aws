default_pulling_interval: 10
token:
platform_host_url: "http://localhost:8080"
plugins:
  - type: s3
    name: s3_adapter
    aws_access_key_id:
    aws_secret_access_key:
    datasets:
      - bucket: my_bucket
        prefix: prefix
